@model IEnumerable<VarietyFootwear.EntityFramework.Models.Feedback>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmailAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Like)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Feedback1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmailAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Like)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Feedback1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MailingListId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MailingListId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MailingListId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
